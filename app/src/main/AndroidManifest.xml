<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tfgluismi">

    <application
        android:name="Data.AppTFGLuismi"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".NavigationDrawer"
            android:label="@string/title_activity_navigation_drawer"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/SplashTheme">
        </activity>
        <activity
            android:name=".AgregarTarea"
            android:parentActivityName=".MostrarTareas" />
        <activity android:name=".MostrarTareas" />
        <activity
            android:name=".AgregarArchCon"
            android:parentActivityName=".MostrarArchCon" />
        <activity android:name=".MostrarArchCon" />
        <activity
            android:name=".AgregarProyecto"
            android:parentActivityName=".MostrarProyectos" />
        <activity android:name=".MostrarProyectos" />
        <activity
            android:name=".AgregarArchProy"
            android:parentActivityName=".MostrarArchProy" />
        <activity android:name=".MostrarArchProy" />
    </application>

</manifest>